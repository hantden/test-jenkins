pipeline {
    agent any

    stages {
        stage('testStage') {
            steps {
                echo 'It is alive..'
app = docker.build("getintodevops/hellonode")

            }
        }
    }
}
